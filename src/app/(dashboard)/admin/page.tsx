import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Admin Panel | Divebar Database",
  description: "Administrative console for managing dive bars, reviews, and community feedback roadmaps.",
  alternates: {
    canonical: "/admin",
  },
};

export default function AdminPage() {
  return null;
}
