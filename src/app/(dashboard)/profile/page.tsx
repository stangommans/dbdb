import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "My Profile & Reviews | Divebar Database",
  description: "View your active dive bar ratings, local taps contribution history, and reviewer tokens.",
  alternates: {
    canonical: "/profile",
  },
};

export default function ProfilePage() {
  return null;
}
