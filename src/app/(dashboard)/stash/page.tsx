import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "My Saved Stash | Divebar Database",
  description: "View and manage your bookmarked local taprooms and custom dive list.",
  alternates: {
    canonical: "/stash",
  },
};

export default function StashPage() {
  return null;
}
