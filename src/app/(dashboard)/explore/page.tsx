import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Explore Dive Bars | Divebar Database",
  description: "Search, filter, and compare ratings, draft sizes, and drink pricing index of local taprooms in real-time.",
  alternates: {
    canonical: "/explore",
  },
};

export default function ExplorePage() {
  return null;
}
