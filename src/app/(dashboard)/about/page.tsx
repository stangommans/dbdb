import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "About DBDB | Divebar Database",
  description: "Learn about the mission, origin story, and global community metrics behind the Dive Bar Database (DBDB). Suggest features on our Kanban roadmap.",
  alternates: {
    canonical: "/about",
  },
};

export default function AboutPage() {
  return null;
}
